#!/bin/sh
# Lint a proposed commit message with commitlint.
set -e
root=$(git rev-parse --show-toplevel)
cd "$root"
if [ -x "$root/node_modules/.bin/commitlint" ]; then
  exec "$root/node_modules/.bin/commitlint" --edit "$1"
fi
exec pnpm exec commitlint --edit "$1"
