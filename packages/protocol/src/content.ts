/** Maximum Unicode code points in the purpose-written plain-text Summary. */
export const SUMMARY_MAX_LENGTH = 240

/** Maximum Unicode code points in the optional canonical Markdown Body. */
export const BODY_MAX_LENGTH = 16 * 1024
